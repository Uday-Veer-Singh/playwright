import { test, expect } from "@playwright/test";

/*
test("test", async ({ page }) => {
  await page.goto("https://www.apple.com/");
  await page.getByRole("button", { name: " Canada (English) " }).click();
  await page.getByRole("menuitem", { name: " Canada (English)" }).click();
  await page.getByRole("button", { name: "Continue" }).click();
  await page.getByRole("link", { name: "Watch the film, MacBook Neo" }).click();
  await page.locator(".ac-slider-thumb-wrapper").click();
  await page.locator(".ac-slider-thumb-wrapper").click();
  await page.locator(".ac-slider-thumb-wrapper").click();
  await page.locator(".ac-slider-thumb-wrapper").click();
  await page.locator(".ac-slider-thumb-background-wrapper").first().click();
  await page.locator(".ac-slider-thumb-wrapper").click();
  await page.locator(".ac-slider-thumb-background").first().click();
  await page.locator(".ac-slider-thumb-wrapper").click();
  await page.locator(".ac-slider-thumb-wrapper").click();
  await page.locator(".ac-slider-thumb-wrapper").click();
  await page.getByRole("button", { name: "Close", exact: true }).click();
  await page.getByLabel("iPhone", { exact: true }).click();
  await page.getByRole("link", { name: "iPhone 17e New" }).click();
  await page
    .getByRole("link", { name: "Pre-order, iPhone 17e" })
    .nth(2)
    .click();
  await page
    .getByRole("listitem")
    .filter({ hasText: "White" })
    .locator("img")
    .click();
  await page
    .getByRole("radio", { name: "256GB  Footnote  1 $899.00 or" })
    .check();
  await page
    .getByRole("radio", { name: "256GB  Footnote  1 $899.00 or" })
    .check();
  await page
    .getByText(
      "Apple Trade In.Get $50–$955 credit towards your new iPhone. Footnote §Select a"
    )
    .click();
  await page
    .getByText(
      "Apple Trade In.Get $50–$955 credit towards your new iPhone. Footnote §Select a"
    )
    .click();
  await page
    .getByText(
      "Apple Trade In.Get $50–$955 credit towards your new iPhone. Footnote §Select a"
    )
    .click();
  await page
    .locator("div")
    .filter({ hasText: /^Continue$/ })
    .nth(1)
    .click();
  await page.getByLabel("Apple", { exact: true }).click();
  await page.getByLabel("Store", { exact: true }).click();
  await page
    .locator(
      "div:nth-child(6) > .rf-cards-scroller-itemview > .rf-productnav-card > .rf-productnav-card-content > .rf-productnav-card-image > img"
    )
    .click();
});
*/
